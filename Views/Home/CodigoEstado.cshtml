@{
    ViewData["Title"] = "Error del sistema";
}

<div class="text-center mt-5">
    <h1 class="text-danger">Error @ViewBag.Codigo</h1>
    <p class="lead">@ViewBag.Mensaje</p>

    <a asp-controller="Home" asp-action="Index" class="btn btn-primary">
        Volver al inicio
    </a>
</div>